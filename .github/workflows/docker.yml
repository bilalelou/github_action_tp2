name: Manual Deployment Simulation

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Simulate Deployment
        run: |
          echo "Starting deployment..."
          echo "Connecting to server..."
          echo "Pulling latest Docker image..."
          echo "Deployment successful!"